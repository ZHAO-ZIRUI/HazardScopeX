[project]
name = "root"
version = "0.0.0"
requires-python = "==3.10.*" # Fit CARLA 0.9.15 Python version requirements.
dependencies = [
    "uv-build>=0.8.10",
]

[tool.uv]
package = false  # Virtual package

[tool.uv.workspace]
members = [
  "core",
  "apps/server",
  "apps/client"
]
