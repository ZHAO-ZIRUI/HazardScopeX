[project]
name = "demo_external_keyboard_control"
version = "0.1.0"
dependencies = [
    "core",
    "opencv-python",
    "open3d",
    "rich",
    "keyboard"
]

[tool.uv.sources]
core = { workspace = true }  # From local workspace